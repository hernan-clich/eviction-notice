# Committed static replay recordings (serialized LedgerData).
# Generate with the /export-replay route, then commit <agent>.json here.
# The /replay route plays these deterministically, without the DB.
